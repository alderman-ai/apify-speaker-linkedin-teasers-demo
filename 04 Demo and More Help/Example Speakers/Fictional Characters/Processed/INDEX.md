# Fictional Characters/Processed/

The five archived speaker folders behind the PNGs in
`../Generated Images/`: `intake.md`, the folder `README.md`, and the logo
and photo each run was built from.

Read one next to its render to see a real filled-in form. One caveat: the
intakes are historical and name the old pre-reorganisation paths — never
take a current path from one.
