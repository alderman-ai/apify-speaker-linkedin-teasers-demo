# Fictional Characters/Processed/

The seven archived speaker folders behind the PNGs in
`../Generated Images/`: `intake.md`, the folder `README.md`, and the logo
and photo each run was built from.

Read one next to its render to see a real filled-in form. Two caveats:
the intakes are historical and name the old pre-reorganisation paths
(never take a current path from one), and `petr-svoboda/` retains only
`README.md` and `intake.md` — its images were not kept.
